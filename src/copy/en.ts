import type { DeepPartial } from '../i18n/merge';
import type { SiteCopy } from './nl';

/**
 * English translation — NOT YET TRANSLATED.
 *
 * Add keys here only when a human has written and approved them. Anything left
 * out falls back to the Dutch original, so a half-finished file never breaks
 * the page. Add `en` to `publishedLocales` in `src/i18n/config.ts` once the
 * translation is complete.
 */
export const en: DeepPartial<SiteCopy> = {
  // TODO: professional English translation.
};
