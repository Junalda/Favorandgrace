import type { DeepPartial } from '../i18n/merge';
import type { SiteCopy } from './nl';

/**
 * Papiamentu translation — NOT YET TRANSLATED.
 *
 * Deliberately empty. Papiamentu must be translated and reviewed by a native
 * speaker from the community; machine translation is not acceptable for this
 * audience and would undermine the point of the site. Until then every key
 * falls back to Dutch and the locale stays out of `publishedLocales`.
 */
export const pap: DeepPartial<SiteCopy> = {
  // TODO: human translation, reviewed by a native Papiamentu speaker.
};
